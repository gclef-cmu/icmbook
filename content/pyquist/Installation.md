(pyquist-installation)=
# Installation

```{note}
Placeholder. Replace with official, tested installation instructions.
```

## Requirements

- Python 3.10 or newer
- An audio output device

## Install

```sh
pip install pyquist        # TODO: confirm the real package name / source
```

To install from the course repository instead:

```sh
git clone https://github.com/jiaweil6/pyquist     # TODO: confirm URL
cd pyquist
pip install -e .
```

## Verify

```python
import pyquist
print(pyquist.__version__)
```

```{seealso}
A student-facing version of these steps is in
{numref}`Section %s <installation>`. Keep the two consistent.
```
