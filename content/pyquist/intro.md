(pyquist)=
# Pyquist Documentation

```{note}
This part is the reference manual for **Pyquist**, the audio library used in
15-322 / 15-622. The API names and signatures below are **illustrative
placeholders** — replace them with the real library once it is finalized, and
keep this section in sync with the code.
```

Pyquist is a Python library for sound synthesis and computer music. Where the
chapters of this book *teach* concepts, this part *documents* the tools.

This reference covers:

- **Overview** — what Pyquist is and how it is organized.
- **Installation** — getting Pyquist running.
- **The sound model** — how Pyquist represents and computes audio.
- **API reference** — functions and classes, grouped by topic.
- **Examples** — short, complete recipes.

```{tip}
New to Pyquist? Start with {numref}`Chapter %s <getting-started>`, then return
here when you need the details.
```
